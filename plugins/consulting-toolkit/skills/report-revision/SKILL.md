---
name: report-revision
description: クライアントレビュー後の既存報告書・提案書を改訂し、版管理・PPT転記までを一連で行うスキル。修正指示の構造化→MD正本更新→PPT反映用差分リスト出力→版管理→章⇔スライド対応表更新→引き継ぎメモ生成のワークフローを実行する。「修正指示を反映して」「レビュー指摘を反映して」「クライアントコメントを反映して」「報告書を改訂して」「報告書を修正して」「PPT反映版を作って」「修正版を作って」「指摘対応を進めて」等のリクエスト時に使用。報告書の新規作成には integrated-analysis-creator を使用すること（本スキルは既存成果物の改訂・版管理のみを対象とする）。
---

# 報告書修正スキル

クライアントレビューの修正指示を既存の報告書（MD正本）に反映し、PPT転記・版管理・章⇔スライド対応・セッション引き継ぎまでを一連で管理する。

## 入出力

- **入力（必須）**: 修正対象のMD正本（例: `最終報告書_v1_0.md`）、修正指示（メール・口頭メモ・議事録・レビューコメント等、形式を問わない）
- **入力（任意）**: 章⇔スライド対応表（既存）、引き継ぎメモ（既存）、現行PPT
- **出力**: 新版のMD正本、修正指示リスト、PPT反映用差分リスト、章⇔スライド対応表（更新）、引き継ぎメモ

## ファイル規約

版の命名・版履歴の記録は [../_shared/versioning-conventions.md](../_shared/versioning-conventions.md) に従う。**このスキルを使うときは必ず最初に読むこと。**

出力ファイルのフォーマットは [references/templates.md](references/templates.md) に定義する。

## ワークフロー

### Step 1: 修正指示の構造化

1. 修正指示のソース（メール・口頭メモ・議事録）を精読し、指摘を1件ずつ分解する。複数論点を含むコメントは指摘単位に分割する
2. 指摘IDを採番する。形式は `R{ラウンド番号}-{連番}`（例: R1-03）。ラウンド番号は反映先のマイナー版番号と揃える（v1_0 → v1_1 への修正なら R1-xx）
3. 「指摘ID／対象章・スライド／指摘内容／対応方針」の表に整理し、`{文書名}_v{X}_{Y}_修正指示.md` として保存する
4. **表をユーザーに提示し、確認を取ってから Step 2 に進む**。対応方針が複数考えられる指摘、意図が読み取れない指摘は対応方針欄を「要確認」とし、質問を添える

### Step 2: 本文MD更新

1. 正本MDをコピーして新版ファイルを作成し、新版側に修正を反映する。**旧版ファイルは編集しない**
2. 反映は指摘ID単位で行い、完了のたびに修正指示リストの状態欄を更新する（反映済み／見送り／要確認）
3. ライティング基準は `../_shared/writing-principles.md` に従う。改訂時は特に:
   - **断定形を維持する**。指摘対応の際に防御的注記・二重留保を追加して逃げない
   - **出典忠実性**。修正で新たに追加するファクト・事例は原典を確認し、原典が使っていないラベル・解釈を貼らない
   - **指摘の射程内にとどめる**。指摘されていない章・記述まで書き換えない。連動修正が必要な場合は、その指摘IDの対応方針に明記したうえで行う
   - **NG語彙・メッセージ品質**。修正で書き起こす文は原則7（NG語彙・言い換え辞書）・原則8（メッセージ・クリスタライズ規範）に従う

### Step 3: PPT反映用差分リスト出力

1. MD正本の変更を「スライド番号×変更種別×変更前×変更後」の対応表に落とし、`{文書名}_v{X}_{Y}_PPT差分.md` として出力する
2. 粒度の基準: **PPTを開いてこの表だけを見て転記作業が完結すること**。「第2章を修正」ではなく「スライド12のメッセージラインを『A』→『B』に差し替え」まで具体化する
3. スライド番号は章⇔スライド対応表（Step 5）を正として引く。対応表が未作成の場合は先に Step 5 で作成する

### Step 4: 版管理

1. versioning-conventions.md に従い、新版を命名・保存する
2. 新版MD末尾の版履歴に1行追加する（版・日付・変更概要・対応指摘ID）

### Step 5: 対応表管理

1. `{文書名}_対応表.md` をプロジェクトディレクトリに生成・更新する。**版ごとに作らず、常に1ファイルを更新する**
2. 対応表は2部構成: (a) 章・節⇔スライド番号⇔スライド内ナンバリング（B-1等）の構成マップ、(b) 指摘ID⇔章⇔スライドの対応状況
3. 章構成・スライド構成・ナンバリングが変わったら、本文より先にこの表を更新する。章とスライドの照合はすべてこの表を正とする

### Step 6: 引き継ぎメモ生成

1. セッション終了時（またはユーザーの指示時）に `引き継ぎメモ.md` をプロジェクトディレクトリに生成・上書き更新する
2. 内容: 背景・目的／現在の版と状態／このセッションで完了したこと／未対応の指摘（IDつき）／次アクション
3. 次セッションが「引き継ぎメモ＋対応表＋修正指示リスト」の3ファイルを読むだけで再開できる状態にする

## 品質チェックリスト

1. すべての指摘IDが「反映済み／見送り／要確認」のいずれかの状態になっているか。無言でスキップした指摘がないか
2. 「見送り」には理由が記載され、ユーザー確認を経ているか
3. MD正本の変更がすべてPPT差分リストに載っているか（差分リストにない本文変更が残っていないか）
4. PPT差分リストのスライド番号・ナンバリングが対応表と一致しているか
5. 版履歴・修正指示リスト・対応表・引き継ぎメモが同じ最新版を指しているか
6. 旧版ファイルを上書き編集していないか
7. 指摘の射程を超えた書き換えをしていないか

## 参考資料

| ファイル | 内容 | 参照タイミング |
| --- | --- | --- |
| [../_shared/versioning-conventions.md](../_shared/versioning-conventions.md) | 版管理規約（命名・版履歴・MD正/PPT派生の原則） | **必ず最初に読む** |
| [references/templates.md](references/templates.md) | 修正指示リスト・PPT差分リスト・対応表・引き継ぎメモの出力フォーマット | 各Stepの出力時 |
